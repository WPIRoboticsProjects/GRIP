    @staticmethod
    def $tMeth.name($step.name())(src, kernel, anchor, iterations, border_type, border_value):
        'Expands area of higher value in an image'
        iterations_int = (int) (iterations +0.5)
        return cv2.dilate(src, kernel, anchor, iterations = iterations_int, borderType = border_type, borderValue = border_value)