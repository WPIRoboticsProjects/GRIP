    @staticmethod
    def $tMeth.name($step.name())(src, max_value, adaptive_method, threshold_type, block_size, c):
        'Applies an adaptive threshold to an array'
        block_int = (int)(block_size + 0.5)
        return cv2.adaptiveThreshold(src, max_value, adaptive_method, threshold_type, block_int, c)