	/**
	 * Computes the weighted addition of two Mats.
	 * @param src1 the first Mat
	 * @param alpha the weight for the first Mat
	 * @param src2 the second Mat
	 * @param beta the weight for the second Mat
	 * @param gamma Constant to add to each sum
	 * @param dst the output Mat
	 */
	private void $tMeth.name($step.name())(Mat src1, double alpha, Mat src2, double beta,
		double gamma, Mat dst) {
		Core.addWeighted(src1, alpha, src2, beta, gamma, dst);
	}