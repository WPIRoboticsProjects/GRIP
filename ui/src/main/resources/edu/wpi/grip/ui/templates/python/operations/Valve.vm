    @staticmethod
    def __valve(boolsw, on_true):
        'return a value if a value is true or None if it is false'
        if(boolsw):
            return on_true
        else:
            return None