	/**
	 * Adds two images where one is multiplied by a number.
	 * @param src1 first image.
	 * @param scale scale for the first image.
	 * @param src2 second image.
	 * @param dst output image
	 */
	public static void CV_scaleAdd(Mat src1, double scale, Mat src2, Mat dst){
		core.scaleAdd(src1, scale, src2, dst);
	}