    @staticmethod
    def $tMeth.name($step.name())(input, hue, sat, val):
        'Segment an image based on hue, saturation, and value ranges'
        out = cv2.cvtColor(input, cv2.COLOR_BGR2HSV)
        return cv2.inRange(out, (hue[0], sat[0] , val[0]),  (hue[1], sat[1] , val[1]))