	/**
	 * Fills a size with given width and height
	 * @param width the width of the size
	 * @param height the height of the size
	 * @param size the size to fill
	 */
	public static void New_Size(MutableOf<Number> width, MutableOf<Number> height, Size size){
		size.height = height.doubleValue();
		size.width = width.doubleValue();
	} 